# Finder
Basic subdomain recon tool written in Python 3 & Nim 


## Usage
`./main example.com /path/to/wordlist`

## Requirements
Python>=3.8 & Nim2.0

## Features
Check through crt.sh entries for subdomains 
Fast asynchronous DNS enumeration written in Nim
